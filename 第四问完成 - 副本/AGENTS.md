## Agent skills

### Issue tracker

本地 Markdown 文件追踪，问题存放在 `.scratch/<功能名>/` 下。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认五个英文标签：needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局：根目录 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
