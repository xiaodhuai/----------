# Antigravity 项目专属规则 (Project Instructions)

这是本项目专属的 AI 行为准则文件。每次对本项目进行操作时，必须强制遵循以下所有规则：

## 核心交互准则 (已生效)
1. **少废话，多干活**：遇到代码问题时，停止长篇大论的理论解释。直接定位文件，直接精准替换有 Bug 的代码。
2. **极简操作流，拒绝弹窗**：除非遇到必须依赖系统终端的场景（如 Git 提交），所有代码编辑任务一律使用专用的文件读写工具进行静默修改，坚决避免非必要的系统弹窗。

## 代码编写规范 (你可以随时修改和补充)
- （示例）禁止在主循环中使用 `HAL_Delay()` 造成阻塞，必须使用基于 SysTick 或定时器的状态机逻辑。
- （示例）涉及到电机 PID 参数修改时，保留旧参数的注释以备对比。
- （示例）所有核心函数的入参和返回值必须有中文说明。

---
> 提示：以后有任何新规定，只需在这个文件里接着往下写，我永远都不会忘！
